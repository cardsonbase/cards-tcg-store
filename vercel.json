/** @type {import('next').NextConfig} */
const nextConfig = {
  framework: "nextjs",
  buildCommand: "npm run build",
  installCommand: "npm install",

  async headers() {
    return [
      {
        source: "/(.*)",  // Apply to all pages
        headers: [
          {
            key: "X-Frame-Options",
            value: "ALLOW-FROM https://cardsonbase.com",  // Allows only your WP site
          },
          {
            key: "Content-Security-Policy",
            value: "frame-ancestors 'self' https://cardsonbase.com https://www.cardsonbase.com;",  // Modern & preferred (add www if needed)
          },
        ],
      },
    ];
  },
};

module.exports = nextConfig;
